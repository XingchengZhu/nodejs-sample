{
  "name": "nodejs-sample",
  "version": "0.0.1",
  "description": "A Node.js sample",
  "main": "app.js",
  "scripts": {
    "pretest": "mkdir -p target",
    "test": "mocha --reporter spec && mocha --reporter mocha-junit-reporter --reporter-options mochaFile=./target/test-results.xml",
    "start": "node app.js"
  },
  "license": "MIT",
  "devDependencies": {
    "mocha": "^1.21.4",
    "should": "^4.0.4",
    "supertest": "^0.14.0",
    "mocha-junit-reporter": "^2.2.1"
  },
  "dependencies": {
    "express": "^4.9.6"
  }
}
